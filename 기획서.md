# 가상 금융 업무 에이전트 — 설계 기획서

자연어 요청으로 계좌·카드 업무를 처리하는 LangGraph 기반 AI Agent.

| 문서 | 위치 |
|---|---|
| 초안 원본 (직접 작성) | [설계 초안/기획서_수기(초안).txt](설계%20초안/기획서_수기(초안).txt) |
| 원본 ↔ 수정 대조 + 근거 | [설계 중/기획서_수기(진행).txt](설계%20중/기획서_수기(진행).txt) |
| 최종 설계 (텍스트) | [설계 중/기획서_수기(정리).txt](설계%20중/기획서_수기(정리).txt) |

---

## 1. 기본 설계 원칙

**1) supervisor 패턴으로 도메인별 Agent를 나눈다.**

**2) 각 Agent는 compile한 subgraph를 supervisor 그래프의 노드로 등록한다.**

```python
builder.add_node("transfer", transfer_graph)   # O
# tool로 감싸지 않는다                          # X
```

`interrupt()`로 멈췄다가 재개하면 **멈춘 줄부터 이어지는 것이 아니라 그 노드를 처음부터 다시 실행**한다. tool로 감싸면 재실행 범위가 tool 함수 전체로 커져서 interrupt 앞의 코드가 두 번 돈다. state 자체는 checkpointer가 저장하므로 잃어버리지 않는다. 문제는 "기억"이 아니라 **재실행 범위**다.

**3) 데이터에 쓰기가 일어나는 모든 업무는 HITL 승인을 받는다.** 읽기만 하는 조회는 승인 없이 출력한다.

> 기준을 "민감정보·돈"으로 잡으면 매번 사람이 판단해야 해서 흔들린다. 카드 분실정지는 돈이 안 움직이고 계좌 별명 변경은 민감정보가 아니지만 둘 다 승인 대상이다. "파일을 바꾸느냐"는 기계적으로 갈린다.

**4) interrupt가 있는 노드에는 부작용을 두지 않는다.** 노드 3개로 쪼갠다.

```python
def propose_node(state):   # 계산만. 저장 안 함
def approve_node(state):   # interrupt() 한 줄만
def execute_node(state):   # 승인된 뒤에만 저장
```

**5) 공통 부분은 agent가 아니라 공통 노드로 만든다.** supervisor의 라우팅 대상이 아니라 모든 흐름 중간에 끼어드는 단계다.

**6) 성공 여부와 금액 등 수치는 Python 함수가 낸 값을 그대로 쓴다.** LLM은 문장만 다듬는다. 성공 여부를 판단하거나 수치를 만들지 않는다.

---

## 2. 전체 구조

![전체 구조](설계%20중/구조/전체_구조.svg)

3단 계층으로 나눈다. **각 층의 선택지를 3~4개로 줄여야 그 층이 자기 도메인 안에서 정확히 판단**하기 때문이다. 층마다 도메인 프롬프트를 따로 쓸 수 있고, 카드 기능을 추가해도 계좌 쪽 프롬프트를 건드리지 않는다.

| 층 | 담당 | 선택지 |
|---|---|---|
| 1단 Supervisor | 도메인 분류 | 계좌 / 카드 |
| 2단 도메인 Agent | 업무 분류 | (계좌) 이체·조회·설정 / (카드) 조회·결제·설정·재발급 |
| 3단 기능 Agent | 종류 분류 + **필요 정보 확인** | 즉시·조건부·나눔 등 |

**필요 정보 확인은 반드시 3단에서 한다.** 2단 시점에는 "이체"까지만 정해지고 유형을 모른다. 즉시는 금액, 조건부는 남길 금액, 나눔은 계좌별 금액 리스트가 필요해서 유형을 모르면 무엇이 부족한지 판단할 수 없다.

**도메인 반송** — 하위 Agent가 "내 도메인 아님"을 반환하면 Supervisor로 되돌려 재분류한다(1회까지). 계층 분류를 쓰는 이상 1단이 잘못 보냈을 때 복구할 길이 있어야 한다.

---

## 3. 실행 흐름

![실행 흐름](설계%20중/구조/실행_흐름.svg)

```
입력
 └ [분기 0] 승인 대기 중인 업무가 있는가?
     ├ 있음 → 응답 해석(승인/거절/취소/수정) → 해당 업무 재개
     └ 없음 → Supervisor(1단) → 도메인 Agent(2단) → 기능 Agent(3단)
         └ [분기 1] 필요 정보가 모두 있는가?
             ├ 없음 → 부족 정보 질문 → 답변 → 분기 1 복귀
             └ 있음 → [분기 2] 실행 가능한가?
                 ├ 불가 → 사유 안내 후 종료
                 └ 가능 → [분기 3] 데이터를 변경하는가?
                     ├ 아니오 → 실행 → 결과 반환
                     └ 예 → 인증 → 처리안 제시 → 승인(interrupt)
                            → 실행 직전 재검증 → 실행 → 저장 → 처리 기록
                            └ [분기 4] 실행/저장 실패 → 변경 전 상태 유지 + 사유 안내
```

### 되돌아오는 경로

| 상황 | 경로 |
|---|---|
| 정보 부족 | 질문 → 답변 → 같은 지점 복귀 |
| 승인 대기 | interrupt → 다음 입력 → **분기 0** → 멈춘 자리에서 재개 |
| 거절 | 실행/저장을 타지 않고 종료 |
| 승인 전 수정 | 처리안 갱신 → 전체 내용 다시 제시 → 승인으로 복귀 |

**분기 0이 없으면 승인이 동작하지 않는다.** 승인 대기 중에 "응, 진행해"를 치면 그게 새 요청으로 분류되기 때문이다.

---

## 4. 계좌

![계좌 기능](설계%20중/기능/계좌_기능.svg)

### 4.1 이체 — TransferAgent

**유형**(3단 분류)과 **축**은 다른 차원이다. 한 요청이 네 개 모두 값을 가진다.

| 차원 | 값 | 정하는 곳 |
|---|---|---|
| 유형 | 즉시 / 조건부 / 나눔 | `transfer_router` |
| 대상 | 본인 / 타인 | 슬롯 |
| 은행 | 자행 / 타행 | 슬롯 |
| 시점 | 즉시 / 예약 | 슬롯 |

> "타인 계좌로 40만원 남기고 나머지를 내일 보내줘" = 조건부 / 타인 / 자행 / 예약

**state**

```
출금 계좌 / 입금 계좌 목록 [{계좌, 금액}] / 이체 유형 / 남길 금액 / 총 이체액
대상 구분 / 은행 구분 / 받는 은행명 / 예금주 이름
비밀번호(검증 후 즉시 폐기) / 예약 일시
잔액 스냅샷 / 처리안 / 승인 상태 / 요청 ID / 실패 사유
```

**분기 조건**

| 조건 | 결과 |
|---|---|
| 이체액 0 이하 | 불가 |
| 원 단위 정수 아님 | 불가 |
| 출금 계좌 잔액 < 총액 | 불가 |
| 출금 계좌 == 입금 계좌 | 불가 |
| 타행인데 은행 미지정 | 질문 |
| 예약 일시가 과거 | 불가 |
| 승인 후 잔액이 달라져 이체액이 바뀜 | **재승인 또는 재요청 안내** |

**처리 규칙**

- 이체 실행 시 거래 내역에 **출금 1건 + 입금 1건**을 기록한다. (빠지면 이후 거래내역 조회가 틀어진다)
- 나눠 이체는 **전부 아니면 전무**. 일부만 반영하지 않는다.
- 예약은 시각이 되면 처리한다. 프로그램이 켜져 있으면 입력을 받을 때마다 확인하고, 꺼져 있었으면 다음 시작 시 지난 예약을 확인한다. 실행 시점에 잔액이 부족하면 실행하지 않고 실패로 남긴다.

### 4.2 조회 — AccountQueryAgent

계좌 목록+잔액 / 잔액 합산 / 거래 내역(기간·금액·유형·검색어)

**규칙**

- 기준일은 프로그램을 켠 날짜(오늘)로 한다. "어제", "이번 주" 는 이 날짜로 계산한다.
- 이번 달 = 기준일이 속한 달, 이번 주 = 월~일, 기간은 시작일·종료일 포함
- 최근순 정렬. 카드 결제 건은 사용한 카드도 함께 표시
- 필터는 함수를 쪼개지 않고 **조건 조합**으로 받는다 ("5만원 이상 결제" = 금액 + 유형)

### 4.3 설정 — AccountSettingAgent

이름(별명) 변경 / 용도 변경 / 계좌 등록 / 등록 계좌 조회·삭제

- 계좌 등록 시 **은행명을 함께 받는다.** 자행·타행을 같은 방식으로 다룬다.
- 용도는 **자유 입력**. 길이 제한만 검사하고 중복은 허용한다.
- 이름은 공백 제거 후 길이 검사 + 같은 소유자 내 중복 불가

---

## 5. 카드

![카드 기능](설계%20중/기능/카드_기능.svg)

에이전트를 4개로 나눈 이유 — 2개로 두면 3단 선택지가 9개·7개가 되어 층을 나눈 목적과 반대가 된다. 조회와 결제는 필요한 정보가 완전히 달라서(결제는 청구 월·결제 계좌·금액·방식·할부 개월) 한 Agent에 두면 필요 정보 확인 단계에서 또 갈라야 한다.

### 5.1 조회 — CardQueryAgent

은행별 카드 조회 / 결제 계좌 / 실 카드 번호 / 멤버십 / 이용 내역

- 조회 결과에 **반드시 상태를 포함**한다. 정지·잠금을 만들어 놓고 상태를 안 보여주면 결과를 확인할 방법이 없다.
- 실 카드 번호는 **마스킹하지 않고 전체**를 보여준다. 본인 인증을 거친 세션이기 때문. 인증이 안 됐으면 인증을 먼저 받는다.
- 이용 내역은 계좌 거래내역 함수를 **카드 필터만 추가해 재사용**한다.

### 5.2 결제 — CardBillingAgent

| 방식 | 뜻 |
|---|---|
| 전체 | 청구 총액 전부 |
| **부분** | 지금 일부만. 남은 금액만 줄어듦. **회차 안 생김** |
| **분할** | 앞으로 N개월에 걸쳐. **미래 회차 생김** |
| 일괄 | 카드 여러 장 한 번에 |

> 부분과 분할은 다른 개념이다. "지금 얼마만" vs "앞으로 몇 번에".

**청구 상태** `unpaid` 미납 / `partial` 일부 납부 / `paid` 납부 완료
남은 금액 = 청구 총액 − 납부한 금액

**저장 규칙** — 여기가 헷갈리기 쉬운 자리다.

| 방식 | 규칙 |
|---|---|
| 전체 / 부분 / 분할 | 한 번에 저장. 하나라도 실패하면 **전부 롤백** |
| **일괄** | **건별 저장.** 2건째가 실패해도 1건째는 유지 |
| (비교) 나눠 이체 | 전부 아니면 전무 ← **일괄과 정확히 반대** |

일괄 결제의 승인은 전체에 대해 한 번만 받고, 결과를 완료/실패/미처리로 나누어 안내한다.

### 5.3 설정 — CardSettingAgent

카드 등록·해지 / 비밀번호 변경 / 분실 신고 / 일시 잠금·해제 / 별칭 변경

![카드 상태 전이](설계%20중/상태/카드_상태전이.svg)

| status | 뜻 |
|---|---|
| `active` | 사용 가능 (초기값) |
| `locked` | 일시 잠금 |
| `lost` | 분실 및 정지 |
| `cancelled` | 해지됨 |

분실·도난·부정사용은 모두 `lost`로 처리하고 **사유(`report_reason`)만 따로 기록**한다. 결과가 "카드를 못 쓰게 만든다"로 같기 때문이다.

**전이 불가 (불가 안내로 빠짐)**

- `lost` → `active` — **분실 정지는 잠금 해제로 되돌릴 수 없다**
- `lost` → `locked`
- `cancelled` → 무엇이든
- 이미 같은 상태 (이미 잠긴 카드를 또 잠그기 등)

**해지는 목록에서 지우지 않고 `cancelled`로 둔다.** 지우면 그 카드로 발생한 이용 내역이 참조를 잃는다.

### 5.4 재발급 — CardReissueAgent

![재발급 상태 전이](설계%20중/상태/재발급_상태전이.svg)

신청 / 신청 조회 / 배송지 수정 / 신청 취소

| status | 수정·취소 |
|---|---|
| `received` 접수 | **가능** |
| `making` 제작중 | 불가 |
| `shipping` 배송중 | 불가 |
| `cancelled` 취소됨 | — |

- 대상 카드가 `lost` 상태여야 신청할 수 있다.
- 취소되지 않은 기존 신청이 있으면 새로 만들지 않고 **기존 신청을 안내**한다.
- 신청을 접수해도 기존 카드의 분실 정지는 **그대로 유지**한다.
- 제작중·배송중 상태는 테스트 데이터로 미리 만들어 제한 동작을 확인한다.
- 실제 제작·배송 진행은 만들지 않는다.

### 5.5 정지 후 재발급 연속 처리

```
CardSettingAgent(분실 정지)   → 승인 1 → 실행 → 저장
        ↓
CardReissueAgent(재발급 신청) → 승인 2 → 실행 → 저장
```

승인은 업무마다 **따로** 받는다. 재발급이 실패하거나 2번째 승인을 거절해도 **이미 완료된 정지는 되돌리지 않는다.**

---

## 6. 공통 노드

![공통 노드](설계%20중/기능/공통_기능.svg)

| 묶음 | 함수 |
|---|---|
| 대상 찾기 | `common_pending_check` `common_check_context` `common_confirm_target` `common_resolve_target` |
| 정보 채우기 | `common_authenticate` `common_ask_missing_information` `common_propose` |
| 승인 | `common_approve` `common_interpret_response` `common_revise` `common_revalidate` |
| 마무리 | `common_save` `common_report` `common_log_request` |
| 실패·예외 | `common_reject` `common_error` `common_reroute` |
| 이어가기 | `common_query_history` `common_next_task` |

### 역할 구분 (헷갈리기 쉬운 것)

| 함수 | 하는 일 |
|---|---|
| 맥락 확인 | "그 카드", "아까 그 계좌"가 무엇인지 **이전 대화에서 찾는다** |
| 사용자 확인 | 확신할 수 없거나 후보가 여러 개일 때 **목록을 주고 고르게 한다** |
| 대상 해소 | 소유자로 거른 뒤 이름을 맞춰 **ID로 바꾼다** (0개 불가 / 1개 확정 / 2개↑ 사용자 확인) |

| 함수 | 하는 일 |
|---|---|
| 저장 | 파일에 쓴다. **실패할 수 있다** |
| 결과 안내 | **저장이 성공했을 때만** 완료를 말한다 |

> 두 개를 한 함수에 두면 저장이 실패했는데 "완료됐습니다"를 출력하게 된다.

### 인증

- 한 번 인증하면 **그 세션 동안 유효**하다. 업무마다 다시 받지 않는다. (세션 = `thread_id` 하나 = 프로그램을 켜고 끌 때까지)
- 인증은 **처리안 제시 전**에 받는다. 승인 직후에 두면 인증이 틀렸을 때 처리안을 다시 만들어야 한다.
- 수단: 계좌 비밀번호 / PIN / 휴대전화번호 / 주민번호 뒷자리 중 택일
- 재시작하면 인증은 풀린다.

### checkpoint

```python
graph = builder.compile(checkpointer=...)   # compile할 때 하나만
```

노드마다 따로 두는 것이 아니다. 모든 노드 경계에서 자동 저장된다. 실행 시 `thread_id`를 넘긴다. subgraph를 노드로 등록하면 부모의 checkpointer를 물려받는다.

### retry / recovery

- **retry는 저장 실패에만** 붙인다. 승인과 실행에는 붙이지 않는다. 승인을 자동 재시도하면 사용자에게 같은 질문을 반복하게 된다.
- **recovery** — 재시작 시 진행 중이던 업무를 확인하고 이어서 처리한다. **이전 승인만으로 변경을 자동 실행하지 않는다.** 꺼져 있는 동안 잔액과 카드 상태가 달라졌을 수 있다.

---

## 7. 로그

3단 라우팅과 interrupt 재개 때문에 **실행 경로가 눈에 안 보인다.** 분류가 틀렸는지, 어느 노드에서 터졌는지, 저장이 어디까지 반영됐는지를 로그 없이는 알 수 없다.

### 남겨야 하는 것

| 종류 | 남길 내용 | 없으면 못 찾는 것 |
|---|---|---|
| `turn` | 사용자 입력 한 번 = 턴 하나. 입력 원문, 총 소요 시간 | 어느 요청에서 터졌는지 |
| `route` | 1단·2단·3단이 각각 무엇을 골랐는지 | 분류가 틀린 층 |
| `node` | 노드 진입·이탈, 소요 시간 | 어디서 멈췄는지 |
| `state` | **바뀐 필드만** before → after | 값이 언제 들어왔는지 |
| `resolve` | 이름 → ID 해소 결과, 후보 개수 | 엉뚱한 계좌를 잡은 이유 |
| `io` | 파일 읽기·쓰기, 성공 여부 | 저장이 실제로 됐는지 |
| `interrupt` | 멈춤 / 재개, 재개 시 **재실행된 노드** | 부작용이 두 번 일어났는지 |
| `error` | 예외 + 그 시점의 state | 터진 지점 |

### 형식

사람이 눈으로 읽는 것이 목적이므로 텍스트로 남긴다.

```
━━━ TURN 3   14:32:10   thread=session-1 ━━━
입력  "생활비에서 저축으로 10만원 옮겨줘"

  [분기0] 승인 대기 없음 → 새 요청
  ▶ supervisor            0.42s   1단 = 계좌
  ▶ account_agent         0.38s   2단 = 이체
  ▶ transfer_router       0.51s   3단 = 즉시
  ▶ resolve_target                "생활비" 후보 1 → acc-001
                                  "저축"   후보 1 → acc-002
  ▶ validate                      OK  이체액 100,000 / 잔액 1,431,800
  ▶ authenticate                  세션 인증 있음 → 건너뜀
  ▶ propose                       state +proposal
  ▶ approve               ⏸ INTERRUPT  승인 대기
━━━ 턴 종료 (승인 대기)  1.31s


━━━ TURN 4   14:32:25 ━━━
입력  "응 진행해"

  [분기0] 승인 대기 있음 (req-0003) → 재개
  ▶ approve  (재실행)             응답 해석 = 승인
  ▶ revalidate                    잔액 1,431,800 변동 없음 OK
  ▶ transfer_execute              acc-001  1,431,800 → 1,331,800
                                  acc-002  2,280,000 → 2,380,000
                                  transactions +2 (tx-021 출금 / tx-022 입금)
  ▶ save                  ✗ 실패  PermissionError: data.json
                                  롤백 → 변경 전 상태 유지
  ▶ error                         안내 "저장에 실패해 이체를 완료하지 못했습니다"
━━━ 턴 종료 (실패)  0.88s
```

`(재실행)` 표시가 중요하다. interrupt에서 재개하면 그 노드가 처음부터 다시 도는 것이 **정상**인데, 이 표시가 없으면 로그만 보고 버그라고 오해하게 된다. 반대로 재실행 표시가 붙은 노드에서 저장이나 기록이 일어났다면 그건 **진짜 버그**다.

### 파일

```
logs/
└── 2026-09-22.log       날짜별 한 파일. 이어 쓴다
```

- 프로그램을 다시 켜도 같은 날짜면 이어 쓴다. 재시작 전후를 한 파일에서 볼 수 있다.
- `logs/`는 `.gitignore`에 넣는다.

### 마스킹

로그는 파일로 남으므로 아래는 값을 찍지 않는다.

| 필드 | 로그 표기 |
|---|---|
| `account_password` `card_password` `pin` | `****` |
| `ssn_tail` | `*******` |
| `phone` | `010-****-5678` |

카드 번호와 계좌번호는 **그대로 남긴다.** 어느 대상을 잡았는지 추적하는 데 필요하고, 가상 데이터이기 때문이다.

### 콘솔과 파일을 분리한다

| 대상 | 기본값 | 내용 |
|---|---|---|
| 콘솔 | **끔** | 대화 화면에 로그가 섞이면 읽기 어렵다 |
| 파일 | 켬 | 전부 남긴다 |

`--debug` 플래그를 주면 콘솔에도 같이 띄운다.

### LangSmith와의 관계

`langsmith`가 이미 의존성에 들어 있다. 둘은 겹치지 않는다.

| | 보는 것 |
|---|---|
| LangSmith | LLM 호출 하나하나의 프롬프트와 응답, 토큰 |
| 파일 로그 | 노드 흐름, state 변화, 파일 IO, interrupt, 예외 |

"LLM이 왜 그렇게 분류했나"는 LangSmith, **"어디서 터졌나"는 파일 로그**가 답한다.

---

## 8. 데이터 구조

기능에 맞춰 필드를 정했다.

| 키 | 필드 |
|---|---|
| `users` | owner_id, name, phone, ssn_tail, pin — 인증에 쓴다 |
| `accounts` | account_id, owner_id, nickname, **purpose**, balance, **bank_code**, **bank_name**, **account_number**, **account_password** |
| `registered_accounts` | registered_id, owner_id, bank_name, account_number, holder_name, nickname |
| `cards` | card_id, owner_id, name, account_id, status, card_number, card_type, **bank_name**, **card_password**, **report_reason**, **credit_limit** |
| `transactions` | transaction_id, owner_id, account_id, type, amount, occurred_at, card_id, merchant — **계좌** 입출금 |
| `card_usages` | usage_id, owner_id, card_id, amount, occurred_at, merchant — **신용카드** 이용 내역 |
| `memberships` | membership_id, owner_id, card_id, name, grade, points |
| `card_statements` | statement_id, card_id, owner_id, billing_month, total_amount, paid_amount, remaining_amount, due_date, status, items[] |
| `card_installments` | installment_id, statement_id, card_id, total_amount, months, paid_count, monthly_amount, status |
| `scheduled_transfers` | schedule_id, owner_id, from_account, to_account, amount, scheduled_at, status |
| `reissue_applications` | application_id, owner_id, card_id, address_id, status, created_at |
| `addresses` | address_id, owner_id, label, address |
| `requests` | request_id, owner_id, task_type, target, content, status, result, created_at |
| `bills` | (초기 제공 데이터. 청구서 카테고리를 제외했으므로 현재 쓰지 않는다) |

**굵게** 표시한 것이 초기 데이터에 추가한 필드다.

### 신용카드 이용 내역을 따로 둔 이유

체크카드는 결제 즉시 계좌에서 빠지므로 `transactions`에 출금으로 남는다.
신용카드는 쓴 시점에 계좌 잔액이 줄지 않고 청구서로 모였다가 결제일에 한 번에 빠진다.
같은 배열에 넣으면 거래내역 조회 결과와 실제 잔액이 맞지 않으므로 `card_usages`로 분리했다.

따라서 **카드 이용 내역 조회는 두 곳을 합쳐서 보여준다.**
체크카드는 `transactions`(card_id 필터), 신용카드는 `card_usages`.

---

## 9. 구현 단계

### 진행 방식

1. **한 단계는 기능 하나 단위**로 구현한다.
2. 구현이 끝나면 **멈춘다.** 동작 확인은 직접 한다.
3. 확인이 끝나면 커밋하고 다음 단계로 간다.
4. 한 단계에서 여러 기능을 한꺼번에 만들지 않는다.

### 0단계 — 기반

- [x] 0-1 **초기 데이터 확장** — 7장의 필드를 `initial_data.json`과 `data.json`에 반영
  - 신용카드 최소 1장 (`card_type: "credit"`), 청구·명세서 데이터
  - 테스트용 재발급 신청 (`making` 제작중 / `shipping` 배송중)
- [x] 0-2 **로그** — 턴/노드/state/IO/interrupt/예외 기록, 마스킹, `logs/` 파일 출력
- [x] 0-3 **`data_store`** — 작업본 보장 / 읽기 / 저장 / 초기화 (동작을 로그로 남긴다)

```python
ensure()      # data.json 없으면 initial_data.json에서 복사
load()        # data.json 읽기
save(data)    # data.json 쓰기
reset()       # initial_data.json을 다시 복사해 초기 상태로
```

> **로그를 data_store보다 먼저 만든다.** data_store가 자기 동작(복사·저장·실패)을
> 로그로 남길 수 있어야 한다. 나중에 만들면 이미 쓴 코드에 로그를 끼워 넣게 된다.
>
> **데이터를 먼저 확정한다.** 지금 데이터에는 은행명·비밀번호·용도·계좌번호·멤버십·청구·예약이
> 전부 없어서 계획한 기능 대부분이 성립하지 않는다. 카드 4장도 전부 체크카드라 요금 결제가 안 된다.
> 또 `reset()`이 제대로 도는지 확인하려면 "작업본을 바꿔놓고 → 초기화 → 원본으로 돌아왔나"를
> 봐야 하는데, 데이터가 최종본이어야 그 확인이 의미가 있다.
>
> `initial_data.json`은 **읽기만 한다.** 모든 조회·변경은 `data.json`에만 일어난다.

### 1단계 — 입력부터 출력까지 관통 (읽기 전용, 승인 없음)

- [x] 1-1 `main.py` 입력 루프 — 같은 세션 유지, 종료 명령
- [x] 1-2 Supervisor 라우터 껍데기 (계좌만 연결)
- [x] 1-3 계좌 목록 + 잔액 조회

> 승인이 없는 가장 단순한 업무로 전체 경로를 먼저 완성한다. 라우팅이 되는지를 맨 마지막이 아니라 여기서 확인한다.

### 2단계 — 변경 흐름 완성 (쓰기 + 승인)

- [x] 2-1 공통 — 대상 해소, 맥락 확인, 사용자 확인, 부족 정보 질문
- [x] 2-2 공통 — 처리안 제시, 승인(interrupt), 응답 해석, 승인 대기 판정
- [x] 2-3 공통 — 저장, 결과 안내, 처리 기록, 오류·복구
- [x] 2-4 즉시이체

> 조회 / 변경 / 승인 / 거절 / 실패 다섯 분기를 여기서 모두 완성한다. 이 단계가 끝나면 이후 기능은 같은 틀에 붙이는 작업이 된다. 공통(HITL)을 뒤가 아니라 앞에 두는 이유는 승인이 checkpointer·interrupt 지점·state 구조를 정하는 뼈대이기 때문이다.

### 3단계 — 계좌 확장

- [x] 3-1 잔액 합산
- [x] 3-2 거래 내역 조회 (기준일 + 필터 조합)
- [x] 3-3 조건부 이체
- [x] 3-4 여러 계좌로 나눠 이체 (전부 아니면 전무)
- [ ] 3-5 인증 (`common_authenticate`)
- [ ] 3-6 계좌 이름 / 용도 변경
- [ ] 3-7 계좌 등록 / 조회 / 삭제
- [ ] 3-8 예약 이체 + 예약 조회·취소

### 4단계 — 카드

- [ ] 4-1 CardAgent 라우팅 + 은행별 카드 조회
- [ ] 4-2 결제 계좌 / 카드 번호 / 멤버십 / 이용 내역
- [ ] 4-3 분실 신고 / 일시 잠금 / 잠금 해제 (상태 전이 검증)
- [ ] 4-4 별칭 변경 / 카드 등록·해지 / 비밀번호 변경
- [ ] 4-5 재발급 신청 + 신청 조회
- [ ] 4-6 배송지 수정 / 신청 취소 (상태별 제한)
- [ ] 4-7 정지 후 재발급 연속 처리 (승인 2회)
- [ ] 4-8 요금 조회 / 명세서 조회
- [ ] 4-9 전체 결제 / 부분 결제
- [ ] 4-10 분할 결제 / 일괄 결제 (건별 저장)

### 5단계 — 통합 점검

- [ ] 5-1 후속 결과 조회 ("아까 이체 됐어?")
- [ ] 5-2 재시작 복구 (이전 승인만으로 자동 실행하지 않음)
- [ ] 5-3 도메인 반송 경로 점검

---

## 10. 초안에서 바뀐 것

직접 작성한 초안을 검토하면서 바꾼 내용이다. 자세한 원본 대조는 [기획서_수기(진행).txt](설계%20중/기획서_수기(진행).txt)에 있다.

| # | 바꾼 것 | 이유 |
|---|---|---|
| 1 | 분기 조건 / 처리 규칙 / 상태 전이 섹션 신설 | 기능 목록만으로는 "언제 실패하고 언제 되돌아가는지"를 적을 자리가 없었다 |
| 2 | 공통 노드를 각 기능 흐름에 배치 | 모아만 두면 어느 업무가 언제 부르는지 알 수 없다 |
| 3 | state에 처리안·승인 상태·요청 ID·실패 사유 추가 | HITL이 핵심인데 그 상태를 담을 자리가 없었다 |
| 4 | 흐름에 되돌아오는 경로 4개 추가 | 성공하는 경우만 한 방향으로 그려져 있었다 |
| 5 | 승인 대기 판정(분기 0)을 맨 앞에 추가 | 없으면 "응, 진행해"가 새 요청으로 분류된다 |
| 6 | 필요 정보 확인을 2단 → 3단으로 이동 | 2단 시점에는 유형을 몰라 무엇이 부족한지 판단할 수 없다 |
| 7 | 도메인 반송 경로 추가 | 1단이 잘못 보내면 복구할 길이 없었다 |
| 8 | 승인을 부작용 없는 단독 노드로 분리 | 재개하면 노드를 처음부터 다시 실행하므로 저장이 두 번 된다 |
| 9 | 카드 상태 / 재발급 신청 상태 정의 | "분실 정지는 해제 불가" 같은 분기 조건을 적으려면 상태가 먼저 있어야 한다 |
| 10 | 카드 에이전트 2개 → 4개 | 2개로 두면 3단 선택지가 9개·7개가 되어 층을 나눈 목적과 반대가 된다 |
| 11 | 청구서 카테고리 도입 후 제외 | 카드 결제와 흐름이 같아 중복. 부분·일괄만 카드로 옮겨 살렸다 |
| 12 | 설계 순서를 기능 단위 관통으로 변경 | 원래 순서는 마지막 통합 전까지 끝까지 도는 업무가 하나도 없었다 |
| 13 | 데이터 구조를 기능에 맞게 확장 | 기능을 정한 뒤 그 기능이 쓸 필드를 정했다 |
| 14 | 이름 정리 | `AcoountAgent`→`AccountAgent`, `regester`→`register`, `SettingAgent`→`AccountSettingAgent`, `check_missing`→`ask_missing` |

---

## 11. 결정 사항

| 항목 | 결정 |
|---|---|
| 인증 유효 기간 | 세션 단위 (프로그램 켜고 끌 때까지) |
| 계좌 용도 | 자유 입력 |
| 실 카드 번호 | 전체 노출 (본인 인증을 거치므로) |
| 타행 이체 상대 계좌 | 등록해서 저장 (은행명 함께 입력) |
| 예약 이체 | 예약 시각에 실제로 실행 |
| 본인 확인 | 기본 인증 단계를 둔다 |
| 분실 / 신고 | 하나로 합침 (`status=lost`, 사유만 기록) |
| 청구서 카테고리 | 제외. 부분·일괄 결제를 카드로 이관 |
| retry | 저장 실패에만 붙인다 |
