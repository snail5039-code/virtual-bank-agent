# 가상 금융 업무 에이전트 미니 프로젝트

### 프로젝트 개요

은행 앱에서 제공하는 계좌 조회, 이체, 카드 분실 정지 등의 기능을 사용자의 자연어 요청으로 처리하는 AI Agent를 구현한다.

### 제출물

다음 항목을 필수로 제출한다.

| 제출물 | 포함할 내용 |
|---|---|
| 실행 코드 | 구현한 프로그램의 코드 |
| 실행 안내 | 실행 방법, 구현한 기능, 확인해 볼 요청 예시, 구현한 기능과 직접 선정한 실패 상황의 동작 확인 결과 |
| 설계 초안 | 먼저 구현할 업무의 요청부터 종료까지의 흐름도, 주요 State 필드와 변경 시점, 조회·변경·승인·거절·실패의 분기 조건 |
| 최종 설계 자료 | 실제 구현에 맞게 갱신한 설계 자료와 초안에서 변경한 내용 및 이유 |
| 회고 | 설계와 구현 과정에서 배운 점, 어려웠던 점과 해결 과정, 남은 한계와 개선하고 싶은 점 |

설계 초안과 최종 설계 자료를 함께 제출한다. 설계 자료와 회고의 형식은 자유이며, 흐름도는 손그림으로 제출해도 된다. 각자 설계의 변경 이유와 최종 흐름 및 분기 조건을 설명할 수 있어야 한다.

### 진행 방식

먼저 구현할 업무를 정하고, 코딩 전에 AI를 사용하지 않고 설계 초안을 직접 작성한다. 처음부터 프로젝트 전체를 설계할 필요는 없으며, 먼저 구현할 업무 흐름과 주요 분기 조건부터 설계한다. 이후 AI를 활용해 설계를 검토·보완할 수 있다.

설계한 흐름을 기능 또는 작업 단위로 나누어 단계별로 구현하고, 기능을 추가하면서 설계도 확장·보완한다. AI를 활용할 때도 전체 구현을 한 번에 요청하지 않고, 현재 단계의 목적과 구현 범위를 구체적으로 전달한다.

각 단계에서 작성한 코드를 이해하고 동작을 확인한 뒤 다음 단계로 진행한다. 단계별 구현과 수정 내용은 Git 커밋으로 남기고, 커밋 메시지에는 어떤 기능을 추가하거나 수정했는지 드러나도록 작성한다.

### 패키지 설치

프로젝트는 uv로 관리하는 가상환경에서 진행한다. 프로젝트 폴더에서 다음 명령어로 패키지를 추가한다.

```powershell
uv add google-genai langchain langchain-google-genai langsmith langgraph python-dotenv  
```

### 파일 구조

제공된 Python 파일에는 역할을 안내하는 주석만 있다. 각 파일에 필요한 코드를 구현하며, 파일 구조는 자유롭게 변경할 수 있다.

```text
프로젝트/
├── financial-agent-mini-project.md
├── data/
│   ├── initial_data.json   # 초기 데이터 원본
│   └── bank_data.json      # 작업용 복사본
└── src/
    ├── main.py            # 사용자 입력과 실행
    ├── graph.py           # State, 노드, 분기와 승인 흐름
    ├── functions.py       # 데이터 조회·변경 업무 함수
    └── data_store.py      # JSON 복사·읽기·저장
```

`data` 폴더에는 원본과 같은 내용의 작업용 복사본이 준비되어 있다. 파일 이름과 위치는 자유롭게 변경할 수 있다.

## 구현 범위

아래 구현 범위는 예시이며, 본인이 사용하는 은행 앱의 기능과 이용 흐름을 참고하여 구현할 업무를 자유롭게 정해도 된다. 조회 기능과 승인 후 데이터를 변경하는 기능을 포함하고, 선택한 기능들이 하나의 업무 흐름으로 연결되도록 설계한다.

### 계좌·이체

| 기능 | 구현 내용 | 사용자 요청 |
|---|---|---|
| 계좌 조회 | 계좌 목록과 계좌별 잔액을 조회한다. | “내 계좌 목록과 잔액을 보여줘.” |
| 잔액 합산 | 계좌 잔액을 합산해 총액을 안내한다. | “내 계좌에 총 얼마 있어?” |
| 거래 내역 조회 | 기간·금액·유형으로 조회하고 최근순으로 보여준다. 카드 결제에는 사용한 카드도 함께 표시한다. 기간은 시작일과 종료일을 포함하며, 미지정 시 전체 내역을 조회한다. “이번 달”은 기준일이 속한 달, “이번 주”는 월요일부터 일요일까지이다. “결제”는 카드를 사용해 지출한 거래이다. | “이번 달 생활비 출금 내역 보여줘.” / “5만 원 이상 결제한 내역 찾아줘.” |
| 즉시이체 | 출금·입금 계좌와 금액을 확인하고 승인 후 이체한다. | “생활비에서 저축으로 10만 원 옮겨줘.” |
| 조건부 이체 | 출금 계좌에 남길 금액으로 이체액을 계산하고 승인 후 처리한다. 남길 금액은 0 이상 정수이며 이체액이 0 이하면 실행하지 않는다. 실행 전 잔액이 달라져 이체액이 바뀌면 다시 승인받거나 재요청을 안내한다. | “생활비에 40만 원 남기고 나머지를 저축해줘.” |
| 여러 계좌로 나눠 이체 | 하나의 출금 계좌에서 여러 입금 계좌로 지정한 금액을 이체한다. 입금 계좌별 금액과 총액을 보여주고 승인 후 처리한다. 각 금액은 0보다 큰 원 단위 정수이며 출금 계좌 잔액은 총액 이상이어야 한다. 전체 이체의 계좌 잔액과 거래 내역을 한 번에 저장하며, 일부 이체만 반영하지 않는다. 검증이나 저장에 실패하면 이체 전 상태를 유지한다. | “생활비에서 저축으로 20만 원, 여행 자금으로 10만 원 보내줘.” |
| 계좌 별명 변경 | 대상 계좌와 새 이름을 확인하고 승인 후 저장한다. 별명은 앞뒤 공백을 제거한 1~20자이며 다른 계좌와 중복되지 않도록 한다. | “여행 자금 계좌 이름을 휴가비로 바꿔줘.” |

### 카드

| 기능 | 구현 내용 | 사용자 요청 |
|---|---|---|
| 카드 조회 | 카드 이름·ID·종류(신용·체크)·상태를 조회한다. | “내 카드 목록과 상태를 보여줘.” |
| 카드 분실 정지 | 대상 카드를 확인하고 승인 후 분실 정지한다. 사용 가능한 카드와 일시 잠금된 카드 모두 분실 정지할 수 있다. | “생활비 카드 잃어버렸어. 정지해줘.” |
| 카드 일시 잠금 | 대상 카드를 확인하고 승인 후 사용 가능한 카드를 일시 잠금 상태로 변경한다. 분실 정지와 구분하며 계좌 잔액과 기존 거래는 변경하지 않는다. | “생활비 카드를 집에 둔 것 같아. 잠깐 잠가줘.” |
| 카드 잠금 해제 | 대상 카드를 확인하고 승인 후 일시 잠금을 해제한다. 분실 정지된 카드는 잠금 해제로 사용 가능 상태로 바꿀 수 없다. | “생활비 카드 찾았어. 잠금 풀어줘.” |
| 카드 재발급 | 분실 정지된 카드의 배송지를 확인하고 승인 후 재발급을 신청한다. 같은 카드에 취소되지 않은 신청이 있으면 기존 신청을 안내한다. 배송지는 집·회사 중 선택하며, 신청을 접수해도 기존 카드의 분실 정지는 유지한다. | “잃어버린 생활비 카드를 집으로 재발급받고 싶어.” |
| 재발급 신청 조회 | 신청 내역에서 배송지와 처리 상태를 안내한다. 조회할 신청이 불명확하면 목록에서 선택받고, 해당 신청이 없으면 기록이 없음을 안내한다. | “카드 재발급 신청 상태 알려줘.” |
| 재발급 신청 수정·취소 | 접수된 신청의 배송지를 변경하거나 신청을 취소한다. 카드 제작이 시작되기 전까지만 가능하며, 변경 또는 취소할 내용을 확인하고 승인 후 처리한다. | “재발급 카드 배송지를 회사로 바꿔줘.” / “재발급 신청 취소해줘.” |
| 정지 후 재발급 | 카드 정지와 재발급을 이어서 처리하되 각각 승인받는다. 재발급 실패나 취소로 이미 완료된 정지를 되돌리지 않는다. | “생활비 카드를 잃어버렸어. 정지하고 재발급해줘.” |

재발급 신청의 제작 중·배송 중 상태는 테스트 데이터로 준비해 조회와 수정·취소 제한을 확인한다. 실제 카드 제작·배송을 진행하는 기능은 구현 범위에 포함하지 않는다.

### 청구서·납부

| 기능 | 구현 내용 | 사용자 요청 |
|---|---|---|
| 미납 청구서 조회 | 아직 납부하지 않은 청구서와 금액을 조회한다. | “아직 안 낸 청구서 보여줘.” |
| 청구서 납부 | 출금 계좌와 청구서 금액을 보여주고 승인 후 전액 납부한다. 잔액이 충분해야 하며, 기한이 지나도 연체료 없이 납부할 수 있다. 이미 납부한 청구서는 다시 처리하지 않는다. 잔액·출금 거래·청구서 상태를 함께 저장한다. | “생활비 계좌에서 전기요금 납부해줘.” |
| 일괄 납부 | 출금 계좌와 선택한 청구서들의 총액을 보여주고 승인받는다. 납기일이 빠른 청구서부터 건별 처리·저장한다. 잔액 부족인 건은 미납으로 유지하고 다음 건을 확인한다. 파일 저장에 실패하면 해당 건을 미반영 상태로 두고 남은 처리를 중단한다. 이미 저장된 납부는 유지하며, 결과는 완료·실패·미처리로 구분해 안내한다. | “생활비 계좌에서 미납 청구서 전부 납부해줘.” |

### 공통 대화·처리

| 기능 | 구현 내용 | 사용자 요청 |
|---|---|---|
| 승인·거절 | 변경할 내용을 보여주고 LangGraph의 중단·재개로 승인 또는 거절을 받는다. | 승인 질문에 “승인” 또는 “거절” |
| 변경 결과 저장·조회 | 승인한 경우 Python 함수로 데이터를 변경하고 JSON에 저장한다. 이후 조회 요청에는 저장된 데이터를 바탕으로 현재 잔액이나 카드 상태 등을 안내한다. | 처리 후 “계좌 잔액을 보여줘.” / “생활비 카드 상태를 알려줘.” |
| 불완전하거나 실행할 수 없는 요청 안내 | 정보가 부족하거나 실행할 수 없는 요청은 이유를 안내한다. 필요한 정보를 포함해 다시 요청하도록 안내해도 된다. | “이체해줘.” / “생활비에서 저축으로 -1만 원 옮겨줘.” |
| 추가 질문 | 요청에 빠진 정보를 질문하고 답변을 반영한다. 후보가 여러 개이면 사용자에게 선택받는다. | “저축으로 10만 원 옮겨줘.” |
| 승인 전 수정 | 처리안을 수정하고 전체 내용을 다시 보여준 뒤 승인받는다. | “아니, 5만 원만.” |
| 자연어 승인·취소 | 자연어 응답을 현재 승인 대기 업무의 승인 또는 취소로 연결한다. | “응, 진행해.” / “취소할게.” |
| 후속 결과 조회 | 대화 맥락으로 사용자가 가리키는 이전 요청을 찾고, 저장된 처리 기록을 바탕으로 해당 요청의 성공·실패·취소 여부와 처리 내용을 안내한다. | “아까 이체가 됐어?” |
| 대화 대상 확인 | 이전 대화의 대상을 확신할 수 없으면 다시 확인하고, 후보가 여러 개이면 사용자에게 선택받는다. | “그 카드 잠가줘.” |
| 재시작·장애 복구 | 프로그램 재시작이나 저장 장애 후 진행 중이던 업무를 확인하고 이어서 처리할 수 있도록 한다. 이전 승인만으로 변경을 자동 실행하지 않는다. | — |

## 데이터 구조

데이터 구조는 구현할 기능에 맞게 자유롭게 변경할 수 있다.

제공된 JSON 파일은 원본으로 보관하고, 복사본을 만들어 조회·변경에 사용한다. 초기화가 필요하면 원본을 다시 복사한다.

`initial_data.json`은 초기 데이터이며, 최상위 객체에 아래 배열을 담고 있다. 금액은 원 단위 정수이고, ID는 각 항목을 식별하는 문자열이다.

| 키 | 내용 | 초기 데이터 |
|---|---|---|
| `accounts` | 계좌 | 5개 |
| `cards` | 카드 | 4개 |
| `transactions` | 계좌별 입출금 거래 | 20건 |
| `addresses` | 재발급 카드 배송지 | 집·회사 2개 |
| `requests` | 업무 요청의 처리 기록 | 빈 배열 |
| `reissue_applications` | 카드 재발급 신청 | 빈 배열 |
| `bills` | 청구서 | 미납 5건 |

### 계좌 — `accounts`

| 필드 | 자료형 | 설명 |
|---|---|---|
| `account_id` | 문자열 | 계좌 ID |
| `owner_id` | 문자열 | 계좌 소유자 ID |
| `nickname` | 문자열 | 계좌 별명 |
| `balance` | 정수 | 현재 잔액 |

서로 다른 소유자의 계좌에 같은 별명이 있으므로, 별명만으로 계좌를 구분할 수는 없다.

### 카드 — `cards`

| 필드 | 자료형 | 설명 |
|---|---|---|
| `card_id` | 문자열 | 카드 ID |
| `card_type` | 문자열 | 카드 종류. `credit`: 신용카드, `debit`: 체크카드. 초기 카드는 모두 체크카드 |
| `owner_id` | 문자열 | 카드 소유자 ID |
| `name` | 문자열 | 카드 이름 |
| `account_id` | 문자열 | 연결된 계좌의 `account_id` |
| `status` | 문자열 | 카드 상태. 초기값은 사용 가능한 상태인 `active` |
| `card_number` | 문자열 | 가상 카드 번호 |

일시 잠금·분실 정지 등 추가 상태의 값은 구현에 맞게 정의한다.

### 거래 내역 — `transactions`

| 필드 | 자료형 | 설명 |
|---|---|---|
| `transaction_id` | 문자열 | 거래 ID |
| `owner_id` | 문자열 | 거래가 발생한 계좌의 소유자 ID |
| `account_id` | 문자열 | 거래가 발생한 계좌의 `account_id` |
| `type` | 문자열 | `deposit`: 입금, `withdrawal`: 출금 |
| `amount` | 정수 | 거래 금액. 입금·출금 모두 양수로 기록 |
| `occurred_at` | 문자열 | 거래 시각. 예: `2026-09-20T18:00:00+09:00` |
| `card_id` | 문자열 또는 `null` | 사용한 카드의 `card_id`. 카드 거래가 아니면 `null` |
| `merchant` | 문자열 또는 `null` | 가맹점 이름. 해당 정보가 없으면 `null` |

거래 시각은 UTC+09:00을 포함한 ISO 8601 형식이다. 초기 거래는 2026년 8~9월에 분포한다. 상대 날짜를 해석할 때 사용하는 기준일은 프로그램에서 정한다.

### 배송지 — `addresses`

| 필드 | 자료형 | 설명 |
|---|---|---|
| `address_id` | 문자열 | 배송지 ID |
| `owner_id` | 문자열 | 배송지 소유자 ID |
| `label` | 문자열 | 배송지 이름: `집`, `회사` |
| `address` | 문자열 | 가상 배송 주소 |

### 청구서 — `bills`

| 필드 | 자료형 | 설명 |
|---|---|---|
| `bill_id` | 문자열 | 청구서 ID |
| `owner_id` | 문자열 | 청구서 소유자 ID |
| `name` | 문자열 | 청구서 이름 |
| `amount` | 정수 | 전액 납부할 금액 |
| `due_date` | 문자열 | 납기일. `YYYY-MM-DD` 형식 |
| `status` | 문자열 | 납부 상태. 초기값은 미납인 `unpaid` |

납부 완료 상태의 값은 구현에 맞게 정의한다.

### 업무 요청·재발급 신청 — `requests`, `reissue_applications`

두 배열은 처음에는 비어 있으며, 항목의 필드 구조는 선택한 기능에 맞게 설계한다.

| 키 | 저장할 내용 |
|---|---|
| `requests` | 요청 식별자, 업무 종류, 처리 대상과 내용, 처리 상태와 결과 등 |
| `reissue_applications` | 신청 식별자, 대상 카드, 배송지, 신청 상태 등 |

계좌·카드·배송지 등의 연결에는 해당 데이터의 ID를 사용한다.
